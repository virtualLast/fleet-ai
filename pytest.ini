[pytest]
markers =
    unit: deterministic pure-function tests
    integration: orchestration and pipeline tests
    semantic: mocked AI semantic validation tests
    prompt_regression: real-model/provider regression tests (excluded from default CI)
